<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.blackpepper.penguin"
    android:versionCode="6"
    android:versionName="0.0.5" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />
    
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar">

        <activity android:name=".android.MainActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".android.SettingsActivity" android:label="@string/app_name" />
        
        <activity android:name=".android.QueueListActivity" android:label="@string/app_name" />

        <activity android:name=".android.LateralQueueNavigationActivity" android:label="@string/app_name" />

        <activity android:name=".android.QueueActivity" android:label="@string/title_activity_queue" />

    </application>
</manifest>